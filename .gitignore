# Dependencies
node_modules/

# Environment (contains secrets — do NOT commit)
.env

# IDE / Editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Build / output
dist/
build/

# Debug logs
*.log
logs/

# Data (runtime generated)
data/history/*.json
!data/history/.gitkeep

# Runtime state (do NOT commit)
data/runtime/
# Forum Bump 状态（显式列出，防止忽略规则变更时漏掉）
data/runtime/forum-bump/state.json
data/runtime/forum-bump/state.json.tmp-*

# Emoji backup (local asset export — do NOT commit)
data/exported-emojis/
